FROM python:3.12-slim

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends qpdf libmagic1 \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    python-multipart \
    pikepdf

COPY scanner.py .

CMD ["uvicorn", "scanner:app", "--host", "0.0.0.0", "--port", "8001"]